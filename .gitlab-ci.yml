image: node:latest

stages:
  - build
  - test
  - clean_up

cache:
  paths:
    - gstproject/node_modules/


run_tests:
  stage: test
  script:
    - npm test

build_and_deploy_prod:
  stage: build
  script:
    - npm install
    - npm run start
    - apt-get install -yqq ruby ruby-dev
    - gem install dpl
    - dpl --provider=heroku --app=gst-project --api-key=$HEROKU_API_KEY
  artifacts:
    paths:
      - gstproject/node_modules
      - gstproject/public/static/dist/
  only:
    - master

clean_up_job:
  stage: clean_up
  script:
    - rm -rf node_modules
    - rm -rf ~/.node-gyp
  when: on_failure
